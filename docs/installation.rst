============
Installation
============

At the command line::

    $ easy_install nobinobi-stats

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nobinobi-stats
    $ pip install nobinobi-stats
